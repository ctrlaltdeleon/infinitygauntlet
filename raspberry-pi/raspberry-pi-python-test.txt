sudo apt-get install ssmtp mailutils // simple mail trasfer protocall
sudo nano /etc/ssmtp/ssmtp.conf
mailhub=smtp.gmail.com:587

// under # The full hostname
hostname=rasberrypi

AuthUser=YourEmail@gmail.com
AuthPass=YourPassword
UseSTARTTLS=YES
UseTLS=YES

// save, exit, go to terminal

echo "Hello inbox" | mail -s "Test" EmailReciever@hotmail.com

// formatted email
nano myemail.py

//body
smtpUser = 'YourEmail@gmail.com'
smtpPAss = 'YourPassword'

toAdd = 'Address@domain.com'
fromAdd = stmpUser

subject = 'Python Test'
header = 'To: ' + toAdd + '\n' + 'From: ' + fromAdd + '\n' + 'Subject: ' + subject
body = 'From within a Python script'

print header + \n' + body

s = stmplib.SMTP('smtp.gmail.com',587)
