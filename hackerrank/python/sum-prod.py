"""
@author: acfromspace
"""

# import numpy

if __name__ == "__main__":

    print("Install numpy!")

    # Input for a 2-d array
    N, M = map(int, input().split())
    # A = numpy.array([input().split() for counter in range(N)], int)
    # print(numpy.prod(numpy.sum(A, axis=0), axis=None))
