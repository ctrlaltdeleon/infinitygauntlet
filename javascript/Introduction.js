// Objects are everywhere in Javascript

// Syllabus:

// Creating our own objects using object literal Notification
// JavaScript classes and constructors
// Inheritance
// Method chaining
// Prototypes & Prototype inheritance

var names = ["hello", "you", "there"];
// console.log(names) returns the object itself along with other properties

names.length;
// 3

names.sort();
// returns the object in alphabetical order

window;
// returns the attributes of the selected window (such as a browser if the console is open)

window.innerWidth;
// 1080

null;
// null

var name = "mario";
name;
// "mario"

name.length;
// 5

var name2 = new String("rex");
name2;
// Declares it as a string object
